import { HeightConversionPipe } from './height-conversion.pipe';

describe('HeightConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
